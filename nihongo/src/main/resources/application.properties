spring.application.name=nihongo
spring.config.import=optional:file:.env

# Server / port (Render provides PORT env)
server.port=${PORT:8080}

# Database (override with Render env)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-wild-credit-afxpnk9t-pooler.c-2.us-west-2.aws.neon.tech:5432/nihongo_db?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_Ldysb5rAWI0F}
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.keepalive-time=300000

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Email (override with your own credentials)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:moonn5124@gmail.com}
spring.mail.password=${MAIL_PASSWORD:klpukjydobdrjgfw}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Frontend origin for CORS
frontend.origin=${FRONTEND_ORIGIN:http://localhost:3000}

# JWT secret (Base64 encoded)
jwt.secret=${JWT_SECRET:5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437}
